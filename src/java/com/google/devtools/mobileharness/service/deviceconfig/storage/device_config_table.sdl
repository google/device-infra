CREATE TABLE IF NOT EXISTS device_config_table (
  device_uuid VARCHAR(64) PRIMARY KEY,
  device_config VARBINARY(32768) NOT NULL -- binary of mobileharness.api.deviceconfig.DeviceConfig
) CHARACTER SET utf8mb4;
